/**
 * The main class that initiates the line-following behavior.
 *
 * @author hamza-okutucu
 */
public class Main {
	
    /**
     * The entry point of the program, which starts the line following behavior by
     * calling the appropriate method from the LineFollowerController class.
     *
     * @param args The command line arguments (not used in this program).
     */
    public static void main(String[] args) {
        LineFollowerController.startLineFollowing();
    }
}